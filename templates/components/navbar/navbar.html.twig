<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ url('homepage') }}">
            <img src="{{ asset('img/logo.png') }}" alt="logo" class="logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ url('homepage') }}">{{ 'navbar.home' | trans }}</a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                {% endif %}
            </ul>

            <div class="ms-auto">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                {% else %}
                    <a class="btn btn-primary me-2" href="{{ url('login') }}">{{ 'login.title' | trans }}</a>
                    <a class="btn btn-outline-primary me-2" href="{{ url('register') }}">{{ 'register.title' | trans }}</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>